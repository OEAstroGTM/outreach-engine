{
  "name": "oe-slack-bot",
  "version": "1.0.0",
  "description": "OE Slack bot — buy infra from Slack via Claude + tool_use",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets": "bash setup-secrets.sh"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
